# HRMS — MongoDB Backend

Same API as the MySQL backend. See [ERD in MySQL README](../backend-mysql/README.md).

## Setup

```bash
cp .env.example .env
npm install
npm run dev
```

Collections: `departments`, `positions`, `employees`, `users`
